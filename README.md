# React-Formik
Implementation of React Formik and Yup(Schema Builder) as a form management and validation library to easily create, manage, and validate the form data/states instead of using controlled components.
